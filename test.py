from cnnClassifier import logging
# This cnnclassifier is available as because we install the setup.py function
# In this we have given the package directory as inside src
logging.info("Welcome to our custom Log")